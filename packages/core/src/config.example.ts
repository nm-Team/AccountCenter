// mongodb settings
const mongo = {
    url: 'mongodb://localhost:27017/',
    db: 'accountcenter',
};

// backend server port
const port = 4000;

// nmTeam AccountCenter url
const centerUrl = 'http://localhost:5173';

export {
    mongo,
    port,
    centerUrl,
};
